<?xml version="1.0" encoding="utf-8"?>
<!-- dot_active.xml
     Solid green circle shown in the status bar when mesh peers are connected.
     Used in: MainActivity (viewStatusDot when peerCount > 0) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <solid android:color="#00C853" />

    <size
        android:width="10dp"
        android:height="10dp" />

</shape>